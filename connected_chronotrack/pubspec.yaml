name: connected_chronotrack
description: Chronomètre multi-participants avec connexion IoT BLE

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # flutter_blue_plus: ^1.31.0  # décommenter pour ESP32 réel

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
